services:
  flash-ui:
    build: .
    container_name: flash-ui
    restart: unless-stopped
    ports:
      - "8020:8020"
    environment:
      GAMES_DIR: /data/games
    volumes:
      - ./games:/data/games
